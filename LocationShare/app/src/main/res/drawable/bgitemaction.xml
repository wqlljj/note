<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <!--rectangle 矩形 ，oval 椭圆，line 横线，ring 圆环-->
    <corners
        android:radius="5dp" />
    <!--angle 为45的倍数 0表示从左到右，90表示从下到上-->
    <!--gradientRadius 渐变半径 仅当tyoe =radial时有效-->
    <!--type 渐变类别 ：线性渐变linear ；扩散渐变(以一个点向外扩散) radial ；扫描渐变(以一个直线一端为原点旋转扩散) sweep-->
    <gradient
        android:angle="315"
        android:centerColor="#71d4a9"
        android:endColor="#7f44a6"
        android:startColor="#80f1a0"
        android:type="linear" />
    <!--width 边界的宽 ；color 边界颜色；dashGap 边界中间长度；dashWidth 边界实线宽 -->
    <stroke
        android:width="2dp"
        android:color="#44c4c2c2" />
    <padding
        android:bottom="20dp"
        android:left="20dp"
        android:right="20dp"
        android:top="20dp" />

</shape>